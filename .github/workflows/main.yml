name: Autograder Action

# This tells the action to run whenever you push code or manually trigger it
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # This is the exact step name the grader is looking for
      - name: 23f3000396@ds.study.iitm.ac.in
        run: echo "Hello, world!"
